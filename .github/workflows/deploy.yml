name: Deploy rupeeflow.co to S3 + CloudFront

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest

    steps:
      # ── 1. Checkout code ──
      - name: Checkout repository
        uses: actions/checkout@v4

      # ── 2. Setup Node.js ──
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # ── 3. Setup pnpm ──
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest

      # ── 4. Cache pnpm store ──
      - name: Cache pnpm store
        uses: actions/cache@v4
        with:
          path: ~/.pnpm-store
          key: ${{ runner.os }}-pnpm-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      # ── 5. Install dependencies ──
      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # ── 6. Build Next.js static export ──
      - name: Build
        run: pnpm run build
        env:
          NODE_ENV: production

      # ── 7. Configure AWS credentials ──
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ secrets.AWS_REGION }}

      # ── 8. Upload JS/CSS/images with long cache (1 year) ──
      - name: Upload assets to S3
        run: |
          aws s3 sync out/ s3://rupeeflow-co-static-site \
            --delete \
            --cache-control "public,max-age=31536000,immutable" \
            --exclude "*.html" \
            --exclude "*.xml" \
            --exclude "*.txt" \
            --exclude "*.json"

      # ── 9. Upload HTML/XML with no cache (always fresh) ──
      - name: Upload HTML to S3
        run: |
          aws s3 sync out/ s3://rupeeflow-co-static-site \
            --delete \
            --cache-control "no-cache,no-store,must-revalidate" \
            --include "*.html" \
            --include "*.xml" \
            --include "*.txt" \
            --include "*.json"

      # ── 10. Invalidate CloudFront cache ──
      - name: Invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id E2MTW01QDLC79U \
            --paths "/*"

      # ── 11. Done ──
      - name: Done
        run: |
          echo "✓ Deployed successfully!"
          echo "  Live at: https://rupeeflow.co"